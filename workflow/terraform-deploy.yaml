name: Terraform

on:
  workflow_call:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Init
        run: cd ${GITHUB_WORKSPACE} && terraform init